[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-trader"
version = "0.1.0"
description = "Research scaffold for Korean opening ORB backtests."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
